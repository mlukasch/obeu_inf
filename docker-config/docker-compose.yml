version: '2'
services:
    jenkins:
        build: ./jenkins
        ports:
            - "18080:8080"
            - "50000:50000"
        environment:
            - INITIAL_ADMIN_PW=test
        volumes:
            -
    #jenkins:
     #   container_name: "jenkins_container"
      #  build: ./jenkins
       # ports:
        #  - "18080:8080"
         # - "50000:50000"

#    nginx:
#        build: /nginx
#        port

